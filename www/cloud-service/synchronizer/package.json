{
  "name": "cloud-service-synchronizer",
  "version": "1.0.0",
  "description": "An example synchronizer that create a container and configure apache to serve a tenant based message",
  "main": "cloud-service-synchronizer.js",
  "scripts": {
    "start": "node cloud-service-synchronizer.js"
  },
  "author": "Matteo Scandolo",
  "license": "Apache 2.0",
  "dependencies": {
    "amqplib": "^0.4.2",
    "bluebird": "^3.4.6"
  }
}
